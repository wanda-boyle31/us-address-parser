{
  "name": "us-address-parser",
  "version": "0.1.0",
  "description": "Validating parser and pretty printer for US mailing addresses.",
  "license": "MIT",
  "private": true,
  "main": "dist/src/address.js",
  "types": "dist/src/address.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist/test"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
